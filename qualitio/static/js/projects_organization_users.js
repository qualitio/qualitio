$(function() {
  $('#organization_users_form').ajaxForm({
    // if(!response.success) {
    //   // $.notification.error(response.message);
    //   $.shortcuts.showErrors(response.data)
    // } else {
    //   $.notification.notice(response.message);
    // }
  });
});
